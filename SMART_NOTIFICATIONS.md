# 🎯 Умные уведомления

## ✨ Как работает

Теперь уведомления в Telegram приходят **только когда собеседник НЕ в чате!**

### Логика:

```
Пользователь А отправляет сообщение
    ↓
Проверка: Пользователь Б в чате?
    ↓
✅ ДА - уведомление НЕ отправляется (он и так видит)
❌ НЕТ - отправляем уведомление в Telegram
```

---

## 🔧 Реализация

### 1. Новый API: `/api/user-activity`

Отслеживает активных пользователей в памяти:

**Actions:**
- `mark-active` - отметить пользователя активным в чате
- `is-active` - проверить активен ли пользователь
- `mark-inactive` - отметить неактивным
- `get-active-users` - список всех активных (для отладки)

**Таймаут:** 30 секунд без активности = пользователь неактивен

### 2. WebApp отслеживает активность

**При открытии чата:**
- Отправляет `mark-active` с userId и chatId
- Каждые 3 секунды обновляет активность

**При закрытии чата:**
- Отправляет `mark-inactive`

**При закрытии приложения:**
- `beforeunload` event → sendBeacon → mark-inactive

### 3. API проверяет перед отправкой

**Алгоритм:**
```typescript
if (!skipNotification) {
  // Проверяем активность получателя
  const isActive = await checkUserActivity(receiverId, chatId);
  
  if (!isActive) {
    // Отправляем уведомление в Telegram
    await sendTelegramNotification();
  } else {
    // Пропускаем - пользователь в чате
    console.log('Получатель активен, уведомление не нужно');
  }
}
```

---

## 📊 Примеры работы

### Сценарий 1: Оба в чате ✅
```
Пользователь А: открыл чат #123
Пользователь Б: открыл чат #123
    ↓
А пишет сообщение → Б видит мгновенно
❌ Уведомление НЕ отправлено (Б активен)
```

### Сценарий 2: Один в чате, другой нет 📨
```
Пользователь А: открыл чат #123
Пользователь Б: НЕ в приложении
    ↓
А пишет сообщение
✅ Уведомление отправлено в Telegram → Б получает push
```

### Сценарий 3: Неактивность 30+ секунд 📨
```
Пользователь Б: открыл чат #123
    ↓ 40 секунд прошло
Пользователь А: пишет сообщение
    ↓
Проверка: последняя активность Б > 30 сек
✅ Уведомление отправлено (считается неактивным)
```

---

## 🐛 Отладка

### Проверить активных пользователей:

```bash
POST /api/user-activity
{
  "action": "get-active-users",
  "params": {}
}
```

**Ответ:**
```json
{
  "data": {
    "users": [
      {
        "userId": "123456789",
        "chatId": 5,
        "lastSeen": 1699012345678,
        "secondsAgo": 12
      }
    ],
    "count": 1
  }
}
```

### Логи в Vercel:

Ищите строки:
- `👤 Пользователь активен:` - отметка активности
- `📱 Активность получателя:` - проверка перед отправкой
- `🔔 Попытка отправить уведомление:` - уведомление отправлено
- `🔕 Уведомление пропущено - получатель активен` - пропущено

---

## ✅ Преимущества

1. **Не спамит** - уведомления только когда нужны
2. **Экономит ресурсы** - меньше запросов к Telegram API
3. **Лучший UX** - пользователь не получает уведомления о том что и так видит
4. **Работает в реальном времени** - активность обновляется каждые 3 сек

---

## 🚀 Готово!

После деплоя система автоматически:
- Отслеживает кто в каком чате
- Отправляет уведомления только неактивным пользователям
- Автоматически очищает неактивных через 30 сек

**Не требует SQL миграций** - всё в памяти сервера!
