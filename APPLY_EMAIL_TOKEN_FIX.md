# 🔧 Миграция: Исправление email токенов на детерминированные

## Проблема

Старые email пользователи были созданы со случайными токенами, которые не совпадают с новой системой детерминированной генерации HMAC-SHA256.

**Ошибка:**
```
Пользователь не найден! userToken: b3dce109eaa78f5e815337a1124b2a669a7c8532b02c1114fa8d93a550d39763
```

## Решение

Эта миграция обновляет все email пользователей с новыми детерминированными токенами, которые:
- Основаны на их email адресе
- Используют HMAC-SHA256 шифрование
- Совпадают с тем, что генерирует новая система аутентификации

## Как применить

### 1. Откройте Neon Console
https://console.neon.tc/ → вашу базу данных → SQL Editor

### 2. Скопируйте SQL из этого файла

Все содержимое `migrations/fix_email_tokens_deterministic.sql`

### 3. Выполните в SQL Editor

Вы должны увидеть результат:
```
NOTICE:  Email user tokens migration completed!
NOTICE:  All email users now use deterministic HMAC-SHA256 tokens
NOTICE:  Token format: HMAC-SHA256(lowercase_trimmed_email + ":email:v1")
```

### 4. Проверьте результат

После миграции запустите:
```sql
-- Посмотреть обновленные токены
SELECT id, email, user_token, auth_method 
FROM users 
WHERE auth_method = 'email'
LIMIT 5;

-- Проверить количество
SELECT COUNT(*) as total_email_users 
FROM users 
WHERE auth_method = 'email';
```

## Что произойдет

### До миграции:
- Email пользователи имели случайные токены
- При попытке установить никнейм система не находила пользователя
- Токены не совпадали между разными запросами

### После миграции:
- Email пользователи получат новые детерминированные токены
- Токен всегда одинаковый для одного email
- Система найдет пользователя при любом запросе
- API nickname, ads, admin будут работать корректно

## Важно!

⚠️ **Это не повлияет на функциональность**, так как:
- Старые сессии все равно устаревают за ~30 дней
- Новые пользователи будут использовать правильные токены с самого начала
- Email пользователи смогут повторно авторизоваться и получить новый правильный токен

## После миграции

1. Приложение автоматически перезапустится (Vercel)
2. Все API начнут работать без ошибок
3. Старые email пользователи смогут авторизоваться заново для получения актуального токена
