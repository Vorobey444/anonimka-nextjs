# 📱 Anonimka Android App - Руководство

## ✅ Что уже сделано:

1. **Capacitor установлен** - фреймворк для создания Android app
2. **Android платформа добавлена** - проект готов к сборке
3. **Разрешения настроены** - камера, галерея, интернет
4. **Гибридный режим** - app открывает anonimka.kz с доступом к камере

## 🚀 Как запустить приложение:

### В Android Studio (для тестирования):

1. Открой Android Studio (уже запущен)
2. Подожди завершения Gradle Sync (прогресс-бар внизу)
3. Нажми зелёный треугольник ▶️ "Run" вверху
4. Выбери эмулятор или подключи телефон через USB

### На реальном телефоне (через USB):

1. **На телефоне**: Настройки → О телефоне → 7 раз нажми "Номер сборки"
2. Настройки → Для разработчиков → Включи "Отладка по USB"
3. Подключи телефон к компьютеру через USB
4. Разреши отладку на телефоне (появится запрос)
5. В Android Studio выбери свой телефон и нажми Run

## 📦 Как собрать APK для установки:

### 1. Debug APK (для тестирования):

```bash
cd "e:\my project\app chat\anonimka-nextjs\android"
.\gradlew assembleDebug
```

Готовый APK будет в:
`android\app\build\outputs\apk\debug\app-debug.apk`

### 2. Release APK (для публикации):

Сначала создай signing key:

```bash
cd "e:\my project\app chat\anonimka-nextjs"
keytool -genkey -v -keystore anonimka-release.keystore -alias anonimka -keyalg RSA -keysize 2048 -validity 10000
```

Потом собери:

```bash
cd android
.\gradlew assembleRelease
```

## 🔄 Как обновить приложение после изменений:

```bash
# 1. Обнови HTML (если менял index.html)
npx cap copy android

# 2. Синхронизируй изменения
npx cap sync android

# 3. Открой в Android Studio
npx cap open android
```

## 📸 Как работает камера:

Приложение открывает anonimka.kz внутри WebView с доступом к:
- ✅ Камера (фото и видео)
- ✅ Галерея (выбор фото)
- ✅ Файловая система
- ✅ Интернет (API работает)
- ✅ Telegram WebApp (если запускать из Telegram)

## 🎨 Как изменить иконку и название:

### Иконка:

1. Создай PNG 512x512px
2. Положи в `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png`
3. Или используй: https://icon.kitchen/

### Название приложения:

Измени в `android/app/src/main/res/values/strings.xml`:

```xml
<string name="app_name">Anonimka</string>
<string name="title_activity_main">Anonimka</string>
```

## 🌐 Как изменить URL сайта:

Измени в `out/index.html`:

```html
<iframe id="app-frame" src="https://anonimka.kz" ...>
```

## 📤 Публикация в Google Play:

1. **Создай аккаунт** Google Play Console ($25 один раз)
2. **Собери Release APK** (см. выше)
3. **Заполни данные**: описание, скриншоты, политика конфиденциальности
4. **Загрузи APK** в консоль
5. **Отправь на модерацию** (2-7 дней)

Политика конфиденциальности уже есть:
`https://anonimka.kz/PRIVACY_POLICY.md`

## 🐛 Частые проблемы:

### "SDK not found"
- Открой SDK Manager в Android Studio
- Установи Android SDK 34

### "Gradle sync failed"
- File → Invalidate Caches → Restart

### "App не запускается"
- Проверь логи: View → Tool Windows → Logcat

### "Камера не работает"
- Проверь разрешения в `AndroidManifest.xml`
- На телефоне дай разрешение вручную: Настройки → Приложения → Anonimka → Разрешения

## 📞 Контакты:

Если что-то не работает - пиши мне, разберёмся! 🚀
