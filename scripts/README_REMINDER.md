# 📨 Рассылка напоминаний пользователям без анкет

## Описание
Скрипт находит пользователей, которые зарегистрировались в приложении, но не создали ни одной анкеты, и отправляет им напоминание через Telegram бота.

## Как использовать

### 1. Установка зависимостей
```bash
pip install python-telegram-bot psycopg2-binary
```

### 2. Настройка переменных окружения

Создайте файл `.env` или установите переменные:
```bash
export TELEGRAM_BOT_TOKEN="your_bot_token_here"
export POSTGRES_URL="your_neon_database_url"
export NEXT_PUBLIC_APP_URL="https://anonimka.online"
```

### 3. Запуск скрипта
```bash
python scripts/send_reminder_to_users_without_ads.py
```

## Варианты сообщений

В скрипте есть 4 варианта текста:

1. **`friendly`** - Дружелюбный стиль
2. **`motivating`** - Мотивирующий (по умолчанию)
3. **`ironic`** - Ироничный/криповатый
4. **`straightforward`** - Прямолинейный

Чтобы изменить стиль, измените переменную в скрипте:
```python
SELECTED_MESSAGE_STYLE = 'ironic'  # или 'friendly', 'straightforward'
```

## Логика работы

1. Ищет пользователей в таблице `users`, у которых нет записей в `ads`
2. Фильтрует только тех, кто зарегистрировался более 24 часов назад
3. Отправляет им сообщение с кнопкой WebApp для создания анкеты
4. Делает паузу 1 секунда между сообщениями (чтобы не словить лимиты Telegram)

## Безопасность

- ✅ Использует официальный Telegram Bot API
- ✅ Обрабатывает ошибки (заблокированный бот, удалённый аккаунт)
- ✅ Не спамит (только пользователи без анкет + 24ч после регистрации)
- ✅ Логирует результаты

## Пример вывода

```
============================================================
🤖 Запуск рассылки напоминаний пользователям без анкет
============================================================

🔍 Поиск пользователей без анкет (регистрация > 24ч назад)...

📊 Найдено пользователей: 15

📤 Начинаем рассылку...

Пользователь 123456789 (Регистрация: 2025-11-20 10:30:00, Страна: KZ)
✅ Отправлено пользователю 123456789
Пользователь 987654321 (Регистрация: 2025-11-19 15:20:00, Страна: RU)
❌ Ошибка отправки пользователю 987654321: Bot was blocked by the user

============================================================
✅ Успешно отправлено: 14
❌ Ошибок: 1
📊 Всего обработано: 15
============================================================
```

## Автоматизация

Для регулярной рассылки можно добавить в cron:
```bash
# Запускать каждый день в 12:00
0 12 * * * cd /path/to/project && python scripts/send_reminder_to_users_without_ads.py
```

Или использовать Vercel Cron Jobs / GitHub Actions для автоматического запуска.
