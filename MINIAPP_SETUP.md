# 🚀 Настройка Telegram Mini App

## Шаг 1: Откройте BotFather

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/mybots`
3. Выберите вашего бота: **@anonimka_kz_bot**

## Шаг 2: Настройте Mini App

1. В меню бота выберите: **Bot Settings**
2. Выберите: **Menu Button**
3. Выберите: **Configure Menu Button**
4. Введите **название кнопки** (например):
   ```
   🚀 Открыть приложение
   ```
5. Введите **URL Mini App**:
   ```
   https://anonimka.kz/webapp
   ```

## Шаг 3: Настройте Web App

1. Вернитесь в **Bot Settings**
2. Выберите: **Mini App Settings** (или **Web App Settings**)
3. Если такого пункта нет, выберите: **Edit Bot** → **Web App**
4. Введите URL:
   ```
   https://anonimka.kz/webapp
   ```

## Шаг 4: Настройте Deep Links (опционально)

Для поддержки `?startapp=` параметров:

1. В **BotFather** отправьте команду:
   ```
   /setminiapp
   ```
2. Выберите бота: **@anonimka_kz_bot**
3. Введите URL:
   ```
   https://anonimka.kz/webapp
   ```

## Шаг 5: Проверка

После настройки:

1. **Обычный запуск**:
   ```
   https://t.me/anonimka_kz_bot
   ```
   Должна появиться кнопка меню "🚀 Открыть приложение"

2. **Реферальная ссылка** (два варианта):
   
   **Вариант A** (Mini App с startapp):
   ```
   https://t.me/anonimka_kz_bot?startapp=ref_<token>
   ```
   Параметр попадёт в `window.Telegram.WebApp.initDataUnsafe.start_param`
   
   **Вариант B** (классический start):
   ```
   https://t.me/anonimka_kz_bot?start=ref_<token>
   ```
   Бот откроет WebApp с URL `?ref=<token>`

## 📋 Что дальше?

После настройки Mini App:

1. ✅ Приложение появится в меню бота (кнопка снизу)
2. ✅ Будет работать `?startapp=` для прямого запуска
3. ✅ Параметры реферальных ссылок будут работать корректно

## 🔧 Альтернативный способ (если нет пункта Mini App)

Если в BotFather нет пункта "Mini App Settings":

1. Используйте **классический вариант** с `/start`
2. Реферальная ссылка:
   ```
   https://t.me/anonimka_kz_bot?start=ref_<token>
   ```
3. Бот обработает параметр и откроет WebApp с `?ref=<token>`
4. Frontend прочитает параметр из URL

---

## ⚡ Текущее состояние

**Работает сейчас**: Классический вариант `/start ref_<token>`
- Бот получает команду
- Открывает WebApp с `?ref=<token>` в URL
- Frontend читает параметр и регистрирует реферала

**После настройки Mini App**: Прямой запуск через `?startapp=`
- Параметр попадает в `start_param` без обработки ботом
- Быстрее и удобнее для пользователя
